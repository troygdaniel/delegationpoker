//  Specs for Take.NoteView
describe("User spec", function() {
  var options = {username:"krishnamurti_" + Date.now().toString(), fullname: "Jiddu Krishnamurti"};
  var user = new User(options);

  describe("User.register()", function () {

    it("PENDING -> will register a new user", function () {
    });
  
  });
  
});